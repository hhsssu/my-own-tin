<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.myroutine.model.dao.UserDao">

	<!-- 사용자 회원가입 -->
	<!-- 회원가입 폼에 따라 값 다르게 넣어야 함 -->
	<insert id="insert" parameterType="User">
		INSERT INTO users (email, nickname, password, gender, age, is_public, is_admin, is_expert)
		VALUES (#{email}, #{nickname}, #{password}, #{gender}, #{age}, #{isPublic}, #{isAdmin}, #{isExpert})
	</insert>

	<!-- 사용자 로그인 -->
	<select id="login" parameterType="Map" resultType="User">
		SELECT *
		FROM users
		WHERE email = #{email} AND password = #{password}
	</select>

	<!-- 사용자 정보 수정 -->
	<update id="update" parameterType="User">
		UPDATE users
		SET password = #{password}, age = #{age}, gender = #{gender}
		WHERE user_id = #{id}
	</update>

	<!-- 사용자 회원탈퇴 -->
	<!-- <delete id="delete" parameterType="int">
		DELETE FROM users
		WHERE user_id = #{id}
	</delete> -->
	<!-- delete_at 만 현재 시각으로 변경하는 쪽으로 구현 -->
	<update id="delete" parameterType="int">
		UPDATE users
		SET delete_at = now()
		WHERE user_id = #{id}
	</update>

	<!-- 사용자 정보 조회 -->
	<!-- <select id="selectOne"></select> -->


</mapper>