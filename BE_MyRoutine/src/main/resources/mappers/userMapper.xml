<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.myroutine.model.dao.UserDao">

	<!-- 사용자 회원가입 -->
	<!-- 회원가입 폼에 따라 값 다르게 넣어야 함 -->
	<insert id="insert" parameterType="User">
		INSERT INTO users (email, nickname, password, gender, age, isPublic, joinDate)
		VALUES (#{email}, #{nickname}, #{password}, #{gender}, #{age}, #{isPublic}, now())
	</insert>

	<!-- 사용자 로그인 -->
	<select id="login" parameterType="Map" resultType="User">
		SELECT *
		FROM users
		WHERE user_id = #{id} AND password = #{password}
	</select>

	<!-- 사용자 정보 수정 -->
	<update id="update" parameterType="User">
		UPDATE users
		SET (수정될 내용)
		WHERE user_id = #{id}
	</update>

	<!-- 사용자 회원탈퇴 -->
	<delete id="delete" parameterType="int">
		DELETE FROM users
		WHERE user_id = #{id}
	</delete>

	<!-- 사용자 정보 조회 -->
	<!-- <select id="selectOne"></select> -->


</mapper>