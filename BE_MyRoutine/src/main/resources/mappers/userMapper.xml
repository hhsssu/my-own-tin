<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.myroutine.model.dao.UserDao">

	<!-- 사용자 회원가입 -->
	<!-- 회원가입 폼에 따라 값 다르게 넣어야 함 -->
	<insert id="insert" parameterType="User">
		INSERT INTO users (email,
		nickname, password, gender, age, is_public, is_admin, is_expert)
		VALUES (#{email}, #{nickname}, #{password}, #{gender}, #{age},
		#{isPublic}, #{isAdmin}, #{isExpert})
	</insert>

	<!-- 사용자 로그인 -->
	<select id="selectOne" parameterType="Map" resultType="User">
		SELECT *
		FROM users
		WHERE email = #{email} AND password = #{password}
	</select>

	<!-- 사용자 정보 수정 -->
	<update id="update" parameterType="User">
		UPDATE users
		<set>
			<if test="password != null">password = #{password},</if>
			<if test="age != null">age = #{age},</if>
			<if test="gender != null">gender = #{gender},</if>
			<if test="part1 != null">part1 = #{part1},</if>
			<if test="part2 != null">part2 = #{part2},</if>
			<if test="workout_time != null">workout_time = #{workoutTime},</if>
		</set>
		WHERE id = #{id}
	</update>

	<!-- 사용자 회원탈퇴 -->
	<!-- <delete id="delete" parameterType="int"> DELETE FROM users WHERE user_id 
		= #{id} </delete> -->
	<!-- delete_at 만 현재 시각으로 변경하는 쪽으로 구현 -->
	<update id="delete" parameterType="int">
		UPDATE users
		SET delete_at = now()
		WHERE id = #{id}
	</update>

	<!-- id를 통한 사용자 조회 -->
	<select id="selectById" parameterType="int">
		SELECT *
		FROM users
		WHERE id = #{id}
	</select>

</mapper>